# 更新日志 (Changelog)

本文档记录「偏头痛记录日历」项目的所有重要变更。

格式遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

## [1.0.0] - 2026-08-16

首个前后端完整版本。从「纯前端 + localStorage」升级为「前后端分离 + SQLite 持久化 + 账号体系」。

### 新增 (Added)

**后端服务（`server/`）**

- 基于 **Node.js + Express + SQLite** 的 REST API 服务，使用 Node 内置 `node:sqlite`，**零原生编译依赖**。
- 用户认证：注册、登录、获取当前用户；密码使用 `bcryptjs` 哈希存储，登录签发 **JWT**（7 天有效）。
- 日历记录接口：保存 / 读取 / 删除每日偏头痛记录，数据按用户隔离。
- 睡眠记录接口：保存 / 读取 / 删除每日睡眠记录，数据按用户隔离。
- Tips 广场接口：列表、详情、评论、点赞；内置 6 条健康科普种子数据。
- AI 助手代理：`/api/ai/chat` 代理 DeepSeek；聊天历史持久化；未配置 Key 时使用服务端本地模拟回复。
- 环境变量配置（`.env`）：端口、JWT 密钥、DeepSeek Key、数据库路径、前端静态托管开关。
- 接口文档 `server/README.md`。

**前端接入后端**

- 新增统一 API 客户端 `api.js`：封装全部接口；后端不可用时自动降级 localStorage。
- 登录/注册/退出走后端（5 个页面统一）；token 自动保存并跨页面复用。
- 日历、睡眠、Tips、评论等数据「上云」，换设备/清缓存不丢失。
- `diary.html`、`my.html` 支持跨设备读取后端数据。
- AI 对话改走后端代理，密钥不再暴露给浏览器；离线时提示「该功能须联网使用」。
- 未登录写数据拦截：写日记、删除、保存睡眠前校验登录态，未登录提示登录/注册。

**开发体验**

- VS Code 调试配置 `.vscode/launch.json`：F5 一键启动后端 + 前端，均可断点调试（Chrome / Edge 双配置）。

### 修复 (Fixed)

- 登录/注册界面在 `index/diary/sleep` 样式缺失：提取到公共 `styles.css`，全站统一。
- 登录/注册模态框无法关闭：补充 X 按钮与点击外部关闭。
- Service Worker 缓存旧文件导致改动不生效：改为「缓存优先 + 后台更新」，缓存版本升级至 v2。
- `tips.html` 底部「我的」导航图标乱码（`�` → `👤`）。
- `tips.html` 评论表单缺失作者输入框导致提交报错：改为登录用户署名、未登录匿名。
- 账号切换后数据串号：登录/退出时清空本地缓存并刷新，确保数据按账号隔离。

### 安全 (Security)

- 用户密码由明文改为 bcrypt 哈希存储。
- DeepSeek API Key 从前端迁移到服务端。
- 个人数据接口增加 JWT 鉴权，未登录返回 401。

### 更改 (Changed)

- 数据存储由浏览器 localStorage 迁移至服务端 SQLite 数据库，支持多端同步。
- 用户体系由「本地明文账号」升级为「服务端加密账号 + JWT 会话」。
