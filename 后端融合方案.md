# 后端融合方案：ESP32 环境数据同步（newest-ui ↔ Data-analysis）

> 目标：把 newest-ui 的「Sync ESP32 data」新前端（已合并）对应的后端接口与数据表，融合进当前 Data-analysis 主线后端，同时**保留**当前已有的 `/api/hardware` 二进制上传 + `/api/readings` + ESP32 固件能力。

---

## 1. 背景：两套并存的后端方案

| 维度 | 当前主线（保留） | newest-ui（融合进来） |
|------|-----------------|----------------------|
| 数据通路 | ESP32 **主动上传**二进制事件 | 浏览器**主动拉取** ESP32 HTTP 端点，再转发 |
| 上传接口 | `POST /api/hardware/events`（octet-stream 二进制） | `POST /health/environment-sync`（JSON） |
| 设备偏好 | 无 | `PUT /health/device-preference` |
| 数据表 | `hardware_devices` `hardware_events` `hardware_samples` `vitals_readings` | `health_device_preferences` `environment_readings` |
| 解析 | `server/lib/binary_codec.js`（CRC32/Header/Sample/Footer） | 前端 `esp32-parser.js` 解析 `[SAMPLE]` 文本 |
| 前端 | — | `esp32SyncBtn` + 设备选择器（assets 图标） |

**结论**：两者**数据表、接口完全不同，互不冲突**，可以并存。融合 = 「当前硬件方案」 + 「newest-ui 环境同步方案」。

---

## 2. 需要改动的后端文件（共 3 个）

### ① `server/routes/health.js` —— 用 newest-ui 版本**整体替换**
已验证：newest-ui 版是当前版的**安全超集**（新增常量/辅助函数/路由，改动处均为增强，无丢失）。
- 新增常量：`MAX_ENVIRONMENT_READINGS_PER_SYNC = 500`、`DEVICE_TYPES = {apple, miband}`
- 新增辅助：`localDateForTimestamp`、`requiredNumber`、`cleanEnvironmentReading`、`devicePreferenceJson`、`environmentLatestJson`
- 新增路由：
  - `PUT /health/device-preference` → 写 `health_device_preferences`
  - `POST /health/environment-sync` → 批量 upsert 到 `environment_readings`
- 增强既有路由：
  - `GET /health/connection` 返回增加 `environmentLatest`、`devicePreference`
  - `GET /health/analysis` 的 `buildAnalysis` 增加 `environmentRows`，并支持环境时区兜底

### ② `server/health-analysis.js` —— 用 newest-ui 版本**整体替换**
`health.js` 调用的 `buildAnalysis({ ..., environmentRows })` 需要新版签名（支持环境读数参与分析）。

### ③ `server/db.js` —— 在**当前版本**基础上补 2 表 + 2 索引（不整体替换，保住 hardware 表）
在当前 db.js 建表段（`health_connections` 之后）追加：

```sql
CREATE TABLE IF NOT EXISTS health_device_preferences (
  user_id INTEGER PRIMARY KEY,
  device_type TEXT NOT NULL,
  display_name TEXT NOT NULL,
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE TABLE IF NOT EXISTS environment_readings (
  id INTEGER PRIMARY KEY AUTOINCREMENT,
  connection_id INTEGER NOT NULL,
  user_id INTEGER NOT NULL,
  source_record_id TEXT NOT NULL,
  recorded_at TEXT NOT NULL,
  local_date TEXT NOT NULL,
  timezone TEXT NOT NULL DEFAULT 'UTC',
  mono_us INTEGER NOT NULL,
  mode TEXT NOT NULL DEFAULT '',
  temperature_c REAL NOT NULL,
  humidity_pct REAL NOT NULL,
  light_lux REAL NOT NULL,
  noise_db REAL NOT NULL,
  created_at TEXT NOT NULL DEFAULT (datetime('now')),
  updated_at TEXT NOT NULL DEFAULT (datetime('now')),
  UNIQUE(connection_id, source_record_id),
  FOREIGN KEY(connection_id) REFERENCES health_connections(id) ON DELETE CASCADE,
  FOREIGN KEY(user_id) REFERENCES users(id) ON DELETE CASCADE
);

CREATE INDEX IF NOT EXISTS idx_environment_user_date
  ON environment_readings(user_id, local_date);
CREATE INDEX IF NOT EXISTS idx_environment_connection_time
  ON environment_readings(connection_id, recorded_at);
```

> 注意：现有 `server/data.db` 是旧库。`CREATE TABLE IF NOT EXISTS` 只建新表、不动旧表，且兼容逻辑（db.js 底部 ALTER）会补列，无需迁移现有数据。

---

## 3. 不需要动 / 保持不变的文件

| 文件 | 原因 |
|------|------|
| `server/server.js` | 已挂载 `/api/health`（routes/health.js）、`/api/hardware`、`/api/readings`；路由路径不变 |
| `server/routes/hardware.js` | 当前 ESP32 二进制方案，保留 |
| `server/routes/readings.js` | 当前逐条读数方案，保留 |
| `server/lib/binary_codec.js` | 当前二进制解析，保留 |
| `server/i18n.js` | 与 newest-ui **无差异**（`diff = 0`） |
| 根目录 `locales.js` | 已是 newest-ui 版（前端合并时已替换），后端新增 error key 已含 |

---

## 4. 新增 / 变更的 API（融合后最终形态）

| 方法 | 路径 | 认证 | 说明 |
|------|------|------|------|
| PUT | `/api/health/device-preference` | JWT | 保存设备类型(`apple`/`miband`) + 显示名 |
| POST | `/api/health/environment-sync` | JWT | 批量写入环境读数（≤500 条/次），幂等 upsert |
| GET | `/api/health/connection` | JWT | 返回增加 `environmentLatest`、`devicePreference` |
| GET | `/api/health/analysis` | JWT | 分析纳入环境读数（`environmentRows`） |

> 现有 `/api/hardware/*`、`/api/readings/*` 全部保留不变。

---

## 5. 前端已就绪（无需再改）

- `sleep.html`：`esp32SyncBtn`、`esp32EndpointModal`、设备选择器（引用 `assets/*.svg`）
- `health-analysis.js`：`syncEsp32Environment()`（fetch ESP32 → `Esp32Parser` 解析 → 分块 `syncEsp32Environment`）
- `api.js`：`updateHealthDevicePreference()`、`syncEsp32Environment()`
- `esp32-parser.js`、`assets/` 图标：均已就位

---

## 6. 验证步骤

1. 起后端：`cd server && npm start`
2. `node --check` 三个改动文件语法
3. 新建连接：`POST /api/health/connections`
4. 存偏好：`PUT /api/health/device-preference {deviceType:"miband", displayName:"..."}`
5. 模拟环境同步：`POST /api/health/environment-sync`（构造 ≤500 条 readings）
6. `GET /api/health/connection` 应返回 `environmentLatest`、`devicePreference`
7. `GET /api/health/analysis` 应把环境读数纳入统计
8. 回归：确认 `/api/hardware/*`、`/api/readings/*` 仍正常（ESP32 二进制方案未受影响）
9. `node --test` 跑后端测试（若 health.test.js 有变更则更新）

---

## 7. 风险与注意事项

| 风险 | 等级 | 对策 |
|------|------|------|
| `db.js` 手改可能破坏现有建表 | 中 | 只在当前 db.js 里**追加**新表，不重排；`node --check` + 启动冒烟 |
| 新旧两套 ESP32 通路并存，语义重叠 | 中 | 明确分工：硬件二进制=事件上报，`/health/environment-sync`=环境读数；前端 Sync 按钮只走后者 |
| `health-analysis.js` 换版影响分析结果 | 低 | 用 newest-ui 配套版，`buildAnalysis` 签名一致 |
| `sw.js` 缓存旧前端 | 低 | 上线时把缓存版本号升到 `v17-moon-icon` 并缓存 `esp32-parser.js`、`assets/` |
| 现有 data.db 无新表 | 低 | `CREATE TABLE IF NOT EXISTS` 自动建新表，无需迁移 |

---

## 8. 执行顺序建议

1. 备份当前 `server/routes/health.js`、`server/health-analysis.js`、`server/db.js`
2. 替换 `health.js`、`health-analysis.js` 为 newest-ui 版
3. 在 `db.js` 追加 2 表 + 2 索引
4. `node --check` 三个文件 + `npm start` 冒烟
5. 按第 6 节验证
