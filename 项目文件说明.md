# Migraine Signal 项目文件说明

本项目是「偏头痛记录日历 / Migraine Signal」——一个偏头痛数据记录与分析平台。本文档按 **网页前端 → 后端/数据库 → Android** 三大部分，逐一说明仓库里的每一个文件。

> 因为后端和 Android 的文件数量较多，这两部分采用「分节概括 + 关键文件说明」的方式组织；网页前端文件则逐一描述。

---

## 目录速览

```
HHCC2026-Data-analysis/
├── *.html / *.js / *.css      # 网页前端（纯原生多页面应用，无框架）
├── server/                    # 后端 + 数据库（Node.js + Express + SQLite）
├── android-companion/         # Android 手机伴侣应用（Kotlin）
├── Dockerfile / compose.yaml  # 容器化部署
└── README.md / CHANGELOG.md   # 说明与更新日志
```

---

## 第一部分：网页前端（根目录）

前端是**原生 HTML + CSS + JavaScript 多页面应用（MPA）**，不使用任何前端框架或构建工具。所有页面通过 `<script src="...">` 方式共享 `locales.js`、`i18n.js`、`api.js`、`site-header.js`。

### 1. HTML 页面（7 个）

| 文件 | 作用 |
|------|------|
| `index.html` | **日历主页**。月/年双视图日历，点击日期弹出模态框记录「偏头痛 / 日记 / 触发因素」，含登录注册模态框、底部小红书风格图标导航。 |
| `diary.html` | **日记页**。以时间线（timeline）视图展示偏头痛日记，跨设备读取后端数据。 |
| `sleep.html` | **健康分析页**。睡眠记录表单（入睡/起床/时长/质量）+ 健康分析卡片 + 图表，可对接 Health Connect 与 ESP32 读数。加载 `health-analysis.css` 与 `health-analysis.js`。 |
| `tips.html` | **Tips 广场**。健康科普帖子的列表、详情、评论、点赞；卡片铺满整页。 |
| `my.html` | **我的**。个人资料、统计图表，引入 CDN 的 `chart.js` 和 `chartjs-chart-heatmap`。 |
| `ai-chat.html` | **AI 助手**。右下角悬浮的聊天窗口（玻璃拟态样式），调用后端 AI 接口。 |
| `test-sleep-fix.html` | 浏览器内调试测试页，用于验证睡眠/健康分析功能修复。 |

### 2. JavaScript 文件

| 文件 | 作用 |
|------|------|
| `script.js` | 日历页核心逻辑：月份切换、年月视图渲染、模态框记录、调用 `ApiClient` 保存/删除记录。 |
| `health-analysis.js` | 健康分析页逻辑：加载分析数据、睡眠表单提交、渲染图表（Chart.js）、连接 Health Connect、展示 ESP32 环境/体征读数。IIFE 封装。 |
| `ai-chat.js` | AI 聊天功能：聊天窗口开关、消息发送、维护本地 `chatHistory`、调用后端 `/api/ai/chat`。 |
| `api.js` | **统一 API 客户端**（IIFE 挂到 `window.ApiClient`）。封装全部后端接口；含 token 管理、后端可用性探测（`/api/health`）、未登录拦截；**后端不可用时自动降级到 localStorage**。 |
| `i18n.js` | 国际化引擎。根据 `localStorage` / `navigator.languages` 选择中/英文，提供 `I18n.t()` 插值翻译。 |
| `locales.js` | 多语言词典。同时导出 `window.MigraineLocales`（浏览器）与 CommonJS（供 `server/` 复用），含 `keys`、`api`、`ai`、`tips` 等分类。 |
| `site-header.js` | 共享页头组件。渲染品牌 Logo、页面标题、登录/注册/退出按钮，注入到带 `data-site-header` 的挂载点。 |

### 3. CSS 样式

| 文件 | 作用 |
|------|------|
| `styles.css` | **全局样式**。CSS 变量（`--text-primary` 等）、毛玻璃效果、底部图标导航、登录/注册模态框、日历、Tips 卡片等全站通用样式。 |
| `health-analysis.css` | 健康分析页专用样式：卡片布局、`health-top-grid` 响应式网格、图表容器等。 |

### 4. Service Worker（PWA）

| 文件 | 作用 |
|------|------|
| `sw.js` | 离线缓存。采用 **stale-while-revalidate**（缓存优先 + 后台更新）；`/api/` 请求始终走网络不缓存；`install`/`activate` 清理旧缓存。缓存版本 `migraine-app-cache-v9-readings`。 |

### 5. 文档类

| 文件 | 作用 |
|------|------|
| `README.md` | 项目总说明：Docker Compose 启动、环境变量、故障排查。 |
| `CHANGELOG.md` | 更新日志，遵循 Keep a Changelog 与语义化版本格式，记录 v1.0.0 的完整变更。 |

### 6. 图片 / 资源

| 文件 | 作用 |
|------|------|
| `health-page-desktop.png` | 健康分析页桌面端截图（测试自动生成）。 |
| `health-page-mobile.png` | 健康分析页移动端截图（测试自动生成）。 |
| `睡眠.png` | 睡眠相关的演示/说明图片。 |

### 7. 测试与配置文件（前端相关）

| 文件 | 作用 |
|------|------|
| `test-health-page.mjs` | 端到端 UI 测试脚本。通过 **Chrome DevTools Protocol（CDP）** 连接无头 Chrome，验证健康分析页桌面/移动两种视口无横向溢出、英文本地化生效，并输出截图。 |
| `package-lock.json` | 顶层锁文件（根目录无 `package.json`，实际依赖在 `server/` 与 `android-companion/` 管理）。 |
| `.vscode/launch.json` | VS Code 调试配置：F5 一键启动后端 + 前端（Chrome / Edge 双配置，均可断点）。 |

---

## 第二部分：后端与数据库（`server/`）

技术栈：**Node.js ≥ 22.5 + Express 4 + SQLite（内置 `node:sqlite`，零原生编译依赖）+ JWT 认证**。数据库文件 `data.db` 首次启动自动创建并预置 6 条 Tips。

```
server/
├── server.js              # 入口：Express 应用、CORS、路由挂载、静态托管、错误处理
├── db.js                  # SQLite 连接 + 建表 + 预置 Tips 数据
├── middleware.js          # JWT 签名/校验中间件
├── i18n.js                # 服务端国际化（按 Accept-Language 返回对应语言消息）
├── builtin-tips.js        # 内置 Tips 的多语言内容 + 数据迁移
├── health-analysis.js     # 健康分析聚合逻辑（关联睡眠/体征/偏头痛）
├── seed-test-user.js      # 生成测试账号(test/test1234) + 30 天模拟数据
├── lib/binary_codec.js    # 解析 ESP32 上传的二进制事件(Header/Sample/Footer + CRC32)
├── routes/                # 各业务路由
├── test/                  # node:test 测试套件
├── package.json           # 依赖与脚本(用 node:test)
└── .env / .env.example    # 环境变量(端口/JWT密钥/DeepSeek/DB路径)
```

### 核心文件逐一说明

| 文件 | 作用 |
|------|------|
| `server.js` | 应用入口。挂载 `cors`、`express.json(15mb)`、`localeMiddleware`；注册 `/api/auth|calendar|sleep|health|tips|hardware|readings|ai` 全部路由；`DEBUG=1` 时打印请求日志；`SERVE_FRONTEND=1` 时同端口托管前端静态文件；404 与统一错误处理。 |
| `db.js` | 数据库初始化。用 `node:sqlite` 的 `DatabaseSync` 连接；开启外键约束；建 12 张表（users、calendar_entries、sleep_records、health_connections、health_daily、health_sleep_sessions、hardware_devices、hardware_events、hardware_samples、vitals_readings、tips、comments、chat_messages）；自动做 ALTER TABLE 迁移；空表时 `seedTips()` 预置 6 条科普 Tips。 |
| `middleware.js` | 提供 `signToken`（签发 7 天 JWT）、`requireAuth`（必须登录）、`optionalAuth`（可选登录）、`extractToken`（解析 `Bearer` 头）。 |
| `i18n.js` | 根据 `Accept-Language` 规范化语言，`req.t(key)` 返回本地化错误消息。 |
| `builtin-tips.js` | 内置 6 条 Tips 定义；`builtInCopy()` 按语言取文案；`migrateBuiltInTipKeys()` 按指纹（作者/日期/标题）识别并回填 `builtin_key`；`serializeTip()` 序列化输出（含模板与多语言覆盖）。 |
| `health-analysis.js` | 健康分析核心算法。聚合「可穿戴睡眠 / 手动睡眠 / 偏头痛日历」数据，计算相关性、平均心率/血氧/步数、睡眠时长等，遵循证据阈值逻辑；支持 7/30/90 天范围。 |
| `seed-test-user.js` | 测试数据脚本。创建 `test/test1234` 用户，灌入 30 天日历、睡眠、健康连接 + 每日健康数据、逐条体征读数与环境读数（模拟 ESP32 上传）。 |
| `lib/binary_codec.js` | ESP32 二进制协议解析（与固件 `binary_codec.cpp` 一致）。事件 = Header(160B) + N×Sample(44B) + Footer(64B)；魔数 `MGEV`/`MEND`；小端整数 + IEEE-754 float32；CRC-32(IEEE 802.3) 校验；提供 `decodeEvent()` 完整解析。 |

### 路由（`routes/`，均为 `/api` 前缀）

| 文件 | 路由 | 作用 |
|------|------|------|
| `auth.js` | `/auth/register` `/auth/login` `/auth/me` | 注册/登录/当前用户。bcrypt 哈希校验密码，登录签发 JWT。 |
| `calendar.js` | `/calendar` `/calendar/:date` | 日历记录 CRUD（偏头痛/日记/触发因素），`ON CONFLICT` upsert。 |
| `sleep.js` | `/sleep` `/sleep/:date` | 睡眠记录 CRUD（入睡/起床/时长/质量）。 |
| `tips.js` | `/tips` `/tips/:id` `/tips/:id/comments` `/tips/:id/like` | Tips 广场：列表/详情/发布/评论(匿名可)/点赞；支持三种模板。 |
| `health.js` | `/health/connections` `/health/sync` `/health/analysis` `/health/connection` | Health Connect 连接管理、批量同步每日健康数据与睡眠会话、健康分析查询、断连。 |
| `hardware.js` | `/hardware/devices` `/hardware/events` `/hardware/events/:id/samples` | ESP32 设备登记(生成设备令牌)、二进制事件上传(幂等去重、事务入库)、事件/样本查询。 |
| `readings.js` | `/readings/environment` `/readings/vitals` | 逐条时间戳读数：环境(ESP32)与体征(心率/血氧/步数)的查询与批量写入。 |
| `ai.js` | `/ai/chat` `/ai/history` | DeepSeek 代理 + 聊天历史持久化；未配置 Key 或失败时降级为服务端本地 mock 回复（带用户发作记录上下文）。 |

### 数据库表结构（12 张表）

| 表 | 用途 |
|----|------|
| `users` | 用户账号，`password_hash` 存 bcrypt 哈希。 |
| `calendar_entries` | 每日偏头痛记录（migraine/diary/triggers），`UNIQUE(user_id,date)`。 |
| `sleep_records` | 每日睡眠记录，含时长字段与质量。 |
| `health_connections` | 用户与 Health Connect 的连接/设备信息。 |
| `health_daily` | 每日健康聚合数据（心率/血氧/步数/睡眠时长/睡眠阶段）。 |
| `health_sleep_sessions` | 睡眠会话明细（来源记录 ID、起止时间、阶段）。 |
| `hardware_devices` | ESP32 设备登记与设备令牌。 |
| `hardware_events` | ESP32 上传的事件元信息（幂等去重键）。 |
| `hardware_samples` | 环境样本（光照/温湿度/噪音，逐条时间戳）。 |
| `vitals_readings` | 体征读数（心率/血氧/步数）。 |
| `tips` | Tips 帖子（含模板与内置 key）。 |
| `comments` | Tips 评论。 |
| `chat_messages` | AI 聊天记录。 |

### 测试（`server/test/`）

| 文件 | 覆盖内容 |
|------|------|
| `hardware.test.js` | ESP32 设备登记、二进制事件上传、幂等、样本入库。 |
| `health.test.js` | 健康分析算法、可穿戴 vs 手动睡眠优先、证据阈值、时区解析。 |
| `readings.test.js` | 环境/体征读数的查询过滤、批量写入、limit 限制。 |
| `i18n.test.js` | 语言规范化、插值、服务端消息。 |

> 运行：`cd server && npm install && npm start`（默认 3000 端口），开发热重启用 `npm run dev`，测试用 `npm test`。

---

## 第三部分：Android 手机伴侣应用（`android-companion/`）

技术栈：**Kotlin 2.2.20 + Android Gradle Plugin (AGP) 8.13.0 + Gradle 8.14.3（KTS 脚本）+ AndroidX + Health Connect API（connect-client 1.1.0）+ Kotlin 协程**。

作用：侧载（sideload）的 Android MVP，读取用户经 **Health Connect** 授权的健康数据，在手机本地按天聚合后上传到后端。**不直接连小米手环蓝牙、不做后台同步**。

```
android-companion/
├── settings.gradle.kts      # 插件仓库配置 + 模块声明(include ":app")
├── build.gradle.kts         # 声明 AGP / Kotlin 插件版本
├── gradle.properties        # Gradle 配置
├── gradle/wrapper/          # Gradle Wrapper(版本 8.14.3)
├── README.md                # Android 应用说明文档
└── app/
    ├── build.gradle.kts     # app 模块构建配置(compileSdk 37, minSdk 28, targetSdk 35)
    ├── proguard-rules.pro   # 混淆规则
    └── src/
        ├── main/
        │   ├── AndroidManifest.xml        # 权限 + Health Connect 权限声明 + 入口 Activity
        │   ├── java/com/migrainesignal/sync/  # Kotlin 源码
        │   └── res/                        # 布局与资源(含中文本地化 values-zh-rCN)
        └── test/                           # JUnit 单元测试
```

### 构建配置文件

| 文件 | 作用 |
|------|------|
| `settings.gradle.kts` | 仓库源（google/mavenCentral/gradlePluginPortal）、`rootProject.name = "MigraineSignalHealthSync"`、`include(":app")`。 |
| `build.gradle.kts`（根） | 声明 `com.android.application 8.13.0` 与 `org.jetbrains.kotlin.android 2.2.20` 插件版本（`apply false`）。 |
| `gradle.properties` | Gradle 构建参数。 |
| `app/build.gradle.kts` | app 模块：`namespace com.migrainesignal.sync`，`compileSdk 37 / minSdk 28 / targetSdk 35`，Java/Kotlin target 17；依赖 AndroidX 与 Health Connect、协程、JUnit。 |
| `gradle/wrapper/gradle-wrapper.properties` | 固定 Gradle 版本 8.14.3。 |
| `app/proguard-rules.pro` | release 混淆规则（MVP 阶段 `isMinifyEnabled = false`）。 |

### Kotlin 源码（`app/src/main/java/com/migrainesignal/sync/`）

| 文件 | 作用 |
|------|------|
| `MainActivity.kt` | 主界面。登录（HttpURLConnection 调 `/api/auth/login`）、Health Connect 权限请求、发现数据源（小米运动类优先排序）、同步上传、退出。用 `lifecycleScope` + `Dispatchers` 处理异步。 |
| `HealthConnectReader.kt` | 封装 Health Connect 客户端：SDK 状态检查、请求/检查权限、分页读取睡眠/心率/血氧/步数记录、`discoverOrigins()` 枚举数据来源、按指定来源读取 90 天数据。 |
| `DailyAggregator.kt` | **本地聚合算法**。把逐条健康记录按本地时区归并到每一天，计算每日心率 min/avg/max、血氧、步数总和、主睡眠段与睡眠阶段时长，生成 `SyncBundle`。纯函数、可单元测试。 |
| `HealthApiClient.kt` | 后端 API 客户端（`HttpURLConnection` + `org.json`）：登录、创建连接、上传每日摘要与睡眠会话；自定义 `ApiException` 携带 HTTP 状态码。 |
| `SecureTokenStore.kt` | JWT 安全存储。用 **Android Keystore** 生成 AES-GCM 密钥加密 token 后再存入 SharedPreferences。 |
| `HealthModels.kt` | 数据模型（SyncBundle、DailySummary、MetricSummary、SleepSummary、SleepUploadSession、TimedValue 等）。 |

### 资源与测试

| 文件 | 作用 |
|------|------|
| `AndroidManifest.xml` | 声明 `INTERNET` 与 Health Connect 读取权限（睡眠/心率/血氧/步数/历史数据）、`VIEW_PERMISSION_USAGE` 入口、允许明文 HTTP（供局域网开发调试）。 |
| `res/layout/activity_main.xml` | 主界面布局。 |
| `res/values/` 与 `res/values-zh-rCN/` | 界面字符串资源（中英文本地化）。 |
| `res/values/colors.xml` `styles.xml` | 主题与配色。 |
| `app/src/test/.../DailyAggregatorTest.kt` | 聚合算法单元测试（JUnit 4）。 |

> 构建：`cd android-companion && ./gradlew :app:assembleDebug`（Windows 用 `.\gradlew.bat`）。

---

## 附录：容器化部署配置

| 文件 | 作用 |
|------|------|
| `Dockerfile` | 基于 `node:24-bookworm-slim`；`npm ci --omit=dev` 装依赖；非 root 用户 `node` 运行；设置 `DB_PATH=/data/data.db`、`SERVE_FRONTEND=1`。 |
| `compose.yaml` | 编排单一服务 `migraine-app`：端口 3000 映射、从 `.env` 注入 `JWT_SECRET`/`DEEPSEEK_API_KEY` 等、命名卷 `migraine-data` 持久化数据、healthcheck 每 5s 请求 `/api/health`。 |
| `.dockerignore` | 排除 `.env`、`node_modules`、`.git`、`server/data.db` 等，避免进入构建上下文。 |
| `.gitignore` | Git 忽略规则（含 `.env`、`data.db`、`node_modules` 等）。 |

---

*本文档由项目代码分析自动整理，覆盖仓库中全部核心文件。*


